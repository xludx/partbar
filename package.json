{
  "name": "PartBar",
  "version": "1.0.0",
  "description": "Displays Particl price on your Macbook's touchbar.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package:mac": "electron-packager . --overwrite --platform=darwin --arch=x64 --icon=img/logo.icns --prune=true --out=build"
  },
  "repository": "https://github.com/xludx/partbar/",
  "author": "juha@particl.io",
  "license": "WTFPL",
  "dependencies": {
    "babel": "^6.23.0",
    "electron": "^1.6.6",
    "electron-fetch": "^1.0.0",
    "expand-tilde": "^2.0.2"
  },
  "devDependencies": {
    "electron-packager": "^8.7.2"
  }
}
